{
  "name": "aquarela-booking",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "dev": "charge serve src/pages",
    "build": "charge build src/pages public",
    "lint": "npx eslint ./ --ignore-path .gitignore",
    "    lint:fix": "yarn lint -- --fix"
  },
  "devDependencies": {
    "@static/charge": "^1.7.0"
  },
  "dependencies": {
    "prop-types": "^15.7.2"
  }
}
